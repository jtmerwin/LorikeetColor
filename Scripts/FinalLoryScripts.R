##scratch
##CodetogetStarted

       





    # bigDataCols <- cbind.data.frame(stringsAsFactors = F, patchSignalReturned[-c(1), 1], as.numeric(patchSignalReturned[-c(1), 2]), patchSignalReturned[-c(1), 3])
            
    # colnames(bigDataCols) <- c("patch", "colVals", "colMod")
    #colConv <- cbind(modelChoice, c("coral4", "darkgoldenrod3", "skyblue3", "gray"))
    #filterList <- grep(pattern = 0.00000, x = bigDataCols[ , 2], value = F)

    #modelChoice <- c("BM", "OU", "kappa", "white")
     #       cols <- mapvalues(bigDataCols[ , 3], from = colConv[,1], to = colConv[,2])
    #        cols[filterList] <- "transparent"
    #        
    #       colnames(bigDataCols) <- c("patch", "colVals")
    #       bigDataJoin <- cbind.data.frame(stringsAsFactors = F, bigDataCols, cols)
    #       joinedSig <- sp::merge(x = patchMapTotal, bigDataJoin, by.x = "PatchName", by.y = "patch", duplicateGeoms = TRUE)
    #       colsSig <- joinedSig@data$colVals
    #      plot(backgroundMap, col = "gray", border = "transparent")
    #      list <- cols
    #      supCols <- (as.numeric(cols))
    #       plot(add = T, joinedSig, col = cols, border = "transparent")
           
           
           
           
           
           
           